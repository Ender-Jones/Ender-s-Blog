# Cloudflare Pages 配置. ⚠ 这是 Pages 项目, 不是 Worker —
# 建项目时走 "Pages · Connect to Git", 不要走 "Workers · 部署命令 wrangler deploy"(会报 Missing entry-point).
# Pages 建完自动部署, 不需要任何 deploy 命令.
name = "ender-s-blog"
compatibility_date = "2026-07-01"
pages_build_output_dir = "dist"

# —— 第二阶段(console 真联网)才解注释 ——
# 现在留着会让第一阶段静态部署失败(database_id 还不存在). 上线后端时:
# 1) CF 控制台建 D1 库 → 把真实 database_id 填进下面 → 解注释这 4 行.
# 2) functions/api/* 里的 EJ_DB 绑定就通了.
# [[d1_databases]]
# binding = "EJ_DB"
# database_name = "enders-blog-worklog"
# database_id = "REPLACE-AT-GO-LIVE"
