# Console 行为契约(/console/)

> 这份文件回答"什么情况下会发生什么、我会看到什么". 它是 console 的设计文档 —
> **以后改 console 行为, 先改这里, 你点头了再动代码**. 与本文件不符的行为都算 bug.
> (2026-07-11 立此存照: 之前没有这份文档, console 对印是黑箱 — 这是流程失误, 不再犯.)

## 一句话模型

你在一块**当日工作区**(D1)上一行一行记 → 睡前一个**仪式**(close the day)把这一天
编成 markdown 提交进 git(唯一本源)→ 站点自动重建. git 里没有的, 都还不算数.

## 录入前缀(打在行首, 决定这行进哪个段)

| 前缀 | 字段 | 会触发什么 | 不会触发什么 |
|---|---|---|---|
| (无) | done | 纯归档 → 当天 `### Done` | — |
| `@` | project | 纯归档 → `### Project`. 语义 = "今天挂在哪条线上"的上下文标签, 给几个月后的自己扫读用 | **不**进 carry-over, 不追踪, 没有开始/完成状态 |
| `=` | decision | 纯归档 → `### Decision` | 同上 |
| `!` | risk | 纯归档 → `### Risk` | 同上 |
| `>` | next | **唯一有行为的字段**: 明天它会出现在 CARRY-OVER 卡里, 逼你给出口 | — |
| `~` | dropped | 归档 → `### Dropped`(删除线, 有尊严的退场) | 录入台没给它 chip — drop 通常发生在分诊, 不在录入 |

## 仪式字段(只在关日时问一次, 不是录入前缀)

- **public**: 一句公开话 → `### Public` → 主页墙下/worklog 页顶/about 三处 NOW 行. 可空.
- **energy**: 1–5 → `<!-- energy: n -->` 注释. 可空 — 找规律用, 不是打分.
- **save point**: 不是新字段 — 它只是帮你补记一条 `> next`. 当天**已有 next 时输入框消失**
  (已备存档点). **非必填**: 空着照样能关日, 代价只是明天没有零思考起点.

## 闸门(只有这两道, save point 不是闸门)

1. **carry-over 分诊闸**: 昨天的 next 没给出口(done / still next / drop)→ 不准关今天. 刻意的.
2. **空日无 close**: 一行都没有就没有"关日"可言.

## 日界与关日

- **04:00 JST 翻日**: 它只决定"这一行算哪天的"(熬夜到凌晨三点算昨天), **没有任何自动关日**.
- **close 永远是手动仪式**, 设计意图 = 今天的最后一个动作, 关完就去睡.
- 忘了关也不炸: 行都在 D1 里躺着 — 但见下面边界 2.

## 已知边界(诚实清单, 2026-07-11 版)

1. **关日后、同一逻辑日内再录入**: 行能进 D1, 但永远到不了 git(再点 close 会被
   409 "already closed" 拒绝), 成为孤儿行. 前端刷新后也不知道"今天已关过".
2. **忘记关日、睡过 04:00**: 昨天的行悬在 D1 工作区; console 只看"今天", 前端没有
   "补关昨天"的入口(后端其实支持指定日期).
3. **关日当晚 carry-over 回显旧项**: carry 候选烤在构建产物里且只取"今天之前"的日子,
   分诊状态随归档消失 → 关日后旧卡复活. 次日自愈. 此时**别点分诊钮**(会触发边界 1).
4. **push 失败无离线缓冲**(已定论): 玫瑰色报错 + 重试钮, 不假装存上了.

## 已拍板(2026-07-12)— 关日即翻页, 待实现

印的原话: "完美的符合我对关日的所有想象". 原则: **录入台永不禁用** — 捕捉随时可用是
这个工具的第一戒律, "爬起来又干活"被拒绝记录才是对 ADHD 最大的背叛.

1. close 成功那一刻, "今天"提前结束: 之后录入的每一行自动归**下一个逻辑日**.
   睡前爬起来干的活, 明早打开 console 已经躺在工作区里.
2. **一个逻辑日最多关一次**: 关日钮锁到 04:00 真正翻日, 显示 "day closed — next close
   unlocks at 04:00". 堵死"连按 close 穿越到几天后".
3. **不做 unclose**: 仪式不可撤销; 真要改已提交的段落, 手写 md 是永远在的兜底.
4. UI 必须把这一切**显在脸上**(印的要求: 行为不能只写在契约里): 关日后捕捉卡切
   "EARLY START — logging for <明日日期>", 列表显示明日已预录的行, carry 卡隐藏,
   md 预览显示明日的块.
5. 忘关睡过 04:00: 开日检测"昨日有未归档行" → 顶部横幅一键**补关昨天**(完整仪式).
6. **字段语义摘要上录入台**: SYNTAX 区每个词旁边带一眼可读的行为注解(> next 会追到明天 ·
   其余纯归档), 契约文档只做深规格 — "每次都要想每个词是干什么的就完蛋了"(印).
7. 实现落点: `GET /api/log` 返回 `closed` 标志; POST 在"今日已关"时记入明日;
   前端按标志切 EARLY START. 上文「已知边界」1/2/3 随实现消失.

## 拟定 · project → wip(印一个字: 换 / 不换)

- 印指出 project 与 done 易重复(早上 "@写论文", 晚上 "done 论文写完了"), 并裁定:
  **迁移成本不进裁决标准, 唯一准则 = 对抗 ADHD 最优**; 若换, 全部 worklog 的词段一并换.
- Claude 的判断: 重复感说明这个词没扛住语义. 最优解是把字段语义改成
  **"摸了但(今天)没完"** — 与 done(完结)天然互斥, 重复按构造消失. 使用规则一句话:
  **当天完得成就只写 done; 完不成/还在跑, 才记一条 wip**. 副作用正中方法论核心:
  "努力了但没有产出"的日子从此有第一等公民的记录位 — 这正是反愧疚设计要的.
- 词的候选: **wip**(### WIP, 语义最锋利, 推荐)> thread/focus(标签感, 与"摸了没完"不全贴).
  前缀字符 `@` 保留("@ 写论文"读作"正挂在…上", 恰好更贴 wip).
- 若印点头"换": 一次做完 — 2026-04/05/07 全部 `### Project` → `### WIP`, 两代解析器、
  console chip、md.js、D1 语义同步; "legacy 原文永不回改"在**词段维度**由印本人解除
  (2026-07-12), 内容行仍一字不动.
